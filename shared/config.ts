export const CharacterSelectConfig = {
    minLength: 2,
    maxLength: 8,
    askForLastName: true,
    noDuplicateNames: true,
    maxCharacters: 3, // Set this to 1, if you want them to instantly join after creating a character
};
